Frontend — Bus Student Management UI,
A lightweight, dependency-free frontend: plain HTML, CSS, and
vanilla JavaScript. No build step, no framework, no bundler.

Project structure,
frontend/
  index.html
  css/
    style.css
  js/
    api.js
    app.js


Frontend : Served by the backend,
The Express server (backend/src/server.js) already serves this directory
as static files. Just run the backend (see backend/README.md) and open: